**Summary**

Phase 2 mostly lines up with current propstore, but one named surface has drifted: `propstore/support_revision/revision_capture.py` does not exist; current one-shot worldline capture is in `propstore/worldline/revision_capture.py` [worldline/revision_capture.py:12]. `TransitionJournal` and `dispatch` already exist in `support_revision` and are the right journal/replay surfaces [support_revision/history.py:172], [support_revision/dispatch.py:17]. `WorldlineDefinitionDocument` is still single-revision only: it has `revision` and `results`, with no `journal` field [families/documents/worldlines.py:145]. CLI worldline commands are split out of the group module, so new commands should not be placed directly in `__init__.py` [tests/test_cli_layout.py:110].

**Per-file findings**

`propstore/worldline/revision_capture.py`: current `capture_revision_state(bound: Any, revision_query: Any) -> WorldlineRevisionState` handles exactly one revision query [worldline/revision_capture.py:12]. It dispatches by `revision_query.operation` string: `expand` calls `bound.expand` [worldline/revision_capture.py:14], `contract` calls `bound.contract` [worldline/revision_capture.py:22], `revise` calls `bound.revise` [worldline/revision_capture.py:30], and `iterated_revise` calls `bound.iterated_revise` [worldline/revision_capture.py:42]. The iterated path records state through `_revision_state_snapshot(bound, state)` [worldline/revision_capture.py:49], which requires `bound.revision_state_snapshot(state)` [worldline/revision_capture.py:74]. BoundWorld provides that method and returns `EpistemicSnapshot.from_state(state)` [world/bound.py:579].

`propstore/support_revision/dispatch.py`: replay dispatch signature is `dispatch(operator, *, state_in, operator_input, policy) -> EpistemicState` [support_revision/dispatch.py:17]. It normalizes `operator` to `JournalOperator` [support_revision/dispatch.py:26], reconstructs state from `EpistemicStateSnapshot.from_mapping(state_in).to_state()` [support_revision/dispatch.py:27], requires `operator_input` mapping [support_revision/dispatch.py:28], and validates the policy snapshot [support_revision/dispatch.py:29]. It supports `ITERATED_REVISE` [support_revision/dispatch.py:31], `REVISE` [support_revision/dispatch.py:48], and `CONTRACT` [support_revision/dispatch.py:71], but not `expand`.

`propstore/support_revision/history.py`: `TransitionOperation` requires `name`, optional `input_atom_id`, `target_atom_ids`, and `parameters` [support_revision/history.py:134]. `TransitionJournalEntry` requires `state_in`, `operation`, `policy_id`, `operator`, `operator_input`, `version_policy_snapshot`, `normalized_state_in`, `normalized_state_out`, and `state_out`; `explanation`, `policy_payload`, and `schema_version` have defaults [support_revision/history.py:172]. The `from_states` constructor takes `EpistemicState` `state_in`/`state_out` plus the operation, policy id, operator, operator input, version policy snapshot, explanation, and optional policy payload [support_revision/history.py:214]. `TransitionJournal` is just `entries: tuple[TransitionJournalEntry, ...] = ()` [support_revision/history.py:304], coerces entries to a tuple [support_revision/history.py:308], checks adjacent hashes in `check_chain_integrity` [support_revision/history.py:311], and replays entries by calling `dispatch(entry.operator, state_in=entry.normalized_state_in, operator_input=entry.operator_input, policy=entry.version_policy_snapshot)` [support_revision/history.py:330].

`propstore/families/documents/worldlines.py`: current field order for `WorldlineDefinitionDocument` is `id`, `targets`, `name`, `created`, `inputs`, `policy`, `revision`, `results` [families/documents/worldlines.py:145]. The optional `revision` field is `revision: WorldlineRevisionQueryDocument | None = None` [families/documents/worldlines.py:152]. The natural source-order insertion point for optional `journal` is after `revision` and before `results` [families/documents/worldlines.py:152]. There are no document-local `to_yaml`/`from_yaml` methods here; the class inherits Quire's strict `DocumentStruct` [families/documents/worldlines.py:145], [quire/documents/schema.py:16], and YAML goes through `encode_document`/`decode_document_path` [quire/documents/codecs.py:34], [quire/documents/schema.py:70].

`propstore/contracts.py`: `DOCUMENT_SCHEMA_CONTRACT_VERSION` is `VersionId("2026.04.30")` [contracts.py:17]. The overrides mapping currently contains no `WorldlineDefinitionDocument` key; adjacent override style is fully qualified class name to date-like `VersionId`, e.g. `"propstore.families.documents.sources.SourceStanceEntryDocument": VersionId("2026.05.01")` [contracts.py:19]. `_document_contract_version` applies an override by full module/class key, otherwise the default [contracts.py:214]. The checked-in manifest currently records `WorldlineDefinitionDocument` at contract version `2026.04.30` [contract_manifests/semantic-contracts.yaml:2762]. I found no `propstore.worldline_definition.vN` key or `v1 -> v2` convention in current propstore.

`propstore/families/registry.py`: `WORLDLINE_PLACEMENT` is a flat YAML namespace under `worldlines` on the current branch [families/registry.py:479]. `WORLDLINE_FAMILY` is the `ArtifactFamily` named `"worldline"` using `WorldlineDefinitionDocument` [families/registry.py:603]. The semantic registry entry keeps key/name `WORLDLINES`/`"worldlines"` and points to `WORLDLINE_FAMILY` [families/registry.py:922]. Adjacent family and registry entries are comment-free [families/registry.py:543], [families/registry.py:858], so the requested registry comment has no existing adjacent comment convention; if added, it belongs next to `WORLDLINE_FAMILY` or the `WORLDLINES` `FamilyDefinition`.

**Schema-bump mechanics**

Update `worldlines.py` first: add the journal document field after `revision` and before `results` to preserve readable source order [families/documents/worldlines.py:152]. Update the domain conversion layer too: `WorldlineDefinition.from_document` currently reads only `revision` and `results` [worldline/definition.py:319], and `to_dict` writes only `revision` then `results` [worldline/definition.py:366]. Update `contracts.py` by adding a `DOCUMENT_SCHEMA_CONTRACT_VERSION_OVERRIDES` entry for `propstore.families.documents.worldlines.WorldlineDefinitionDocument`; current inherited value is `2026.04.30` [contracts.py:17], [contracts.py:214]. Update the checked-in contract manifest because `test_checked_in_contract_manifest_is_current` compares generated YAML byte-for-byte [tests/test_contract_manifest.py:128], and `test_contract_manifest_changes_require_version_bumps_against_head` checks contract changes against HEAD [tests/test_contract_manifest.py:135]. Update `registry.py` only for the requested explanatory comment; current family contract version is still `ARTIFACT_FAMILY_CONTRACT_VERSION` [families/registry.py:603].

**CLI insertion**

Current inventory is `__init__.py`, `materialize.py`, `display.py`, `mutation.py`, and `rendering.py` [cli/worldline/__init__.py:144], plus the file list observed in `propstore/cli/worldline/`. The root CLI registers the `worldline` group lazily through `_COMMANDS` [cli/__init__.py:13], [cli/__init__.py:45], and `_LazyCLIGroup.get_command` imports only the selected command module [cli/__init__.py:73]. The worldline package defines the group in `__init__.py` [cli/worldline/__init__.py:72], imports sibling command modules after shared helpers [cli/worldline/__init__.py:144], and tests forbid `@worldline.command` in the group module [tests/test_cli_layout.py:122]. Existing commands use Click decorators, `@click.pass_obj`, app-layer request/report objects, `fail`/`ClickException` for errors, and `propstore.cli.output` emitters [cli/worldline/materialize.py:110], [cli/worldline/display.py:27], [cli/worldline/mutation.py:14]. Put `build-journal` and `at-step` in a new sibling module, e.g. `journal.py`, and import it from `__init__.py`; both are journal operations, not materialize/show/delete siblings.

**Backcompat test pattern**

Existing backcompat tests assert old/missing fields parse to defaults, e.g. `RenderPolicy.from_dict({})` yields PRAF defaults [tests/test_praf_integration.py:349]. Existing worldline round trips are dict/document based: `WorldlineDefinition.from_dict(...).to_dict()` round-trips [tests/test_worldline.py:455], revision query blocks preserve `revision` through `WorldlineDefinition` [tests/test_worldline_revision.py:14], and repository family storage round-trips `definition.to_document()` through `repo.families.worldlines` [tests/test_artifact_store.py:116]. Phase 2's backcompat test should use an old-shaped worldline payload with no `journal`, decode/convert it through `WorldlineDefinitionDocument` and `WorldlineDefinition.from_document`, and assert the field is absent/default while `revision` still survives. For YAML specifically, use Quire `encode_document`/`decode_document_bytes` or repository family save/load; there are no direct `to_yaml`/`from_yaml` methods on `WorldlineDefinitionDocument` [quire/documents/codecs.py:34], [quire/documents/schema.py:46].

**Gotchas**

The plan path `propstore/support_revision/revision_capture.py` is stale; current file is `propstore/worldline/revision_capture.py`. `support_revision.dispatch.dispatch` cannot replay `expand`, while current `capture_revision_state` can capture `expand` [support_revision/dispatch.py:87], [worldline/revision_capture.py:14]. `TransitionJournalEntry` requires both normalized state mappings and snapshot objects; building only `(state_in, operation, state_out, policy_payload)` is insufficient for the actual constructor [support_revision/history.py:172]. `version_policy_snapshot` must include `revision_policy_version`, `ranking_policy_version`, and `entrenchment_policy_version` [support_revision/history.py:61]. `DocumentStruct` forbids unknown fields, so adding `journal` to YAML before adding it to the schema will fail strict decode [quire/documents/schema.py:16], [tests/test_worldline.py:433].
